openapi: 3.0.1
info:
  title: JMSProducer
  description: API Definition of JMSProducer
  version: 1.0.0
servers:
- url: http://localhost:8290/jms-producer/1.0.0
paths:
  /jsonPayload:
    post:
      requestBody:
        description: Sample Payload
        content:
          application/json:
            schema:
              type: object
              properties:
                payload:
                  type: object
        required: false
      responses:
        default:
          description: Default response
